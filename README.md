# term-trace
*Trace your terminal sessions, annotate them inline, and generate structured summaries.*

`term-trace` is a CLI tool that logs all commands and outputs from your terminal sessions, allows inline notes, and summarizes activity into human-readable formats (Google Docs, Markdown, etc.).